# ---- NEVER commit credentials ----
.streamlit/secrets.toml

# Local run artifacts
run.log
run.err
*.log

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor
.DS_Store
Thumbs.db

# Company data snapshot stays local only (never pushed to GitHub).
# Live mode (configured via Streamlit Cloud secrets) does not need it.
data/snapshot.csv
